@model ProdutosModel

@{
	ViewData["Title"] = "Criar Produtos";
}

<div class="text-center">
	<h1 class="display-4">Criar Produtos</h1>
</div>
<br />

<form asp-controller="GestaoProdutos" asp-action="Criar" method="post">
	<div class="form-group">
		<label for="SKU">SKU</label>
		<input type="number" asp-for="Sku" class="form-control" placeholder="Insira o SKU do produto">
		@Html.ValidationMessageFor(x => x.Sku)
	</div>
	<div class="form-group">
		<label for="Produto">Produto</label>
		<input type="text" asp-for="Produto" class="form-control" placeholder="Insira o nome do produto">
		@Html.ValidationMessageFor(x => x.Produto)
	</div>
	<div class="form-group">
		<label for="Descricao">Descrição</label>
		<input type="text" asp-for="Descricao" class="form-control" placeholder="Insira a descrição do produto">
		@Html.ValidationMessageFor(x => x.Descricao)
	</div>
	<div class="form-group">
		<label for="Categoria">Categoria</label>
		<input type="number" asp-for="Categoria" class="form-control" placeholder="Insira o categoria do produto">
		@Html.ValidationMessageFor(x => x.Categoria)
	</div>
	<div class="form-group">
		<label for="Fornecedor">Fornecedor</label>
		<input type="number" asp-for="Fornecedor" class="form-control" placeholder="Insira o fornecedor do produto">
		@Html.ValidationMessageFor(x => x.Fornecedor)
	</div>

	<div class="form-group">
		<label for="Quantidade">Quantidade</label>
		<input type="number" asp-for="Qtd" class="form-control" placeholder="Insira a categoria do produto">
		@Html.ValidationMessageFor(x => x.Qtd)
	</div>
	<div class="form-group">
		<label for="Preco">Preço unitário</label>
		<input type="number" asp-for="PrecoUnit" class="form-control" placeholder="Insira o preço do produto" step="0,01">
		@Html.ValidationMessageFor(x => x.PrecoUnit)
	</div>

	<br />
	<button type="submit" class="btn btn-secondary">Guardar</button>
	<a class="btn btn-primary" asp-controller="GestaoProdutos" asp-action="Index" role="button">Voltar</a>


	@*     <script>
    document.addEventListener("DOMContentLoaded", function() {
        document.querySelector("form").addEventListener("submit", function() {
            let qtdInput = document.querySelector("input[name='qtd']");
            let precoInput = document.querySelector("input[name='preco']");
            let fornecedorInput = document.querySelector("input[name='fornecedor']");
            if (qtdInput.value.trim() === "") {
                qtdInput.value = "0"; // Define um valor padrão
            }

            if (precoInput.value.trim() === "") {
                precoInput.value = "0"; // Define um valor padrão
            }

            if (fornecedorInput.value.trim() === "") {
                fornecedorInput.value = "0"; // Define um valor padrão
            }
        });
    });
</script> *@

</form>

@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
}